{
  "dependencies": {
    "html2canvas": "^1.4.1",
    "react-scripts": "^5.0.1",
    "react-webcam": "^7.2.0"
  },
  "scripts": {
    "build": "cd front && npm run build && cd .. && cd back && cd photo && python manage.py collectstatic --noinput"
  }
}
